spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.bookly: DEBUG
    org.hibernate.SQL: DEBUG

app:
  security:
    trusted-proxies: []   # No proxies in dev — X-Forwarded-For always ignored
